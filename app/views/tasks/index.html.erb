<h1 style="margin-left: 7px">Task List</h1>
<p><%= link_to "Add task", "/tasks/new" %></p>
<% @tasks.each.with_index do |task, i| %>
  <h4>&nbsp&nbsp<%= "#{i + 1}. " %>&nbsp<%= link_to task.title, tasks_path + "/#{task.id}" %></h4>
<% end %>