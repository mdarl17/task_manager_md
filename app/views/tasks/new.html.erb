<h1>Add a New Task</h1>
<%= form_with url: "/tasks", method: :post, data: { turbo: false }, local: true do |f| %>
  <%= f.label "title" %>
  <%= f.text_field :title %>

  <%= f.label "description" %>
  <%= f.text_field :description %>

  <%= f.submit "Add task" %>
<% end %>